//! Debug overlay rendering. For v0.1, mostly a stub: a DebugRenderer type that
//! can draw axis-aligned lines (for showing chunk boundaries) and points (for
//! showing the cursor target). Activated by feature flag or runtime toggle.
//! v0.3+ will draw stress field heatmaps and broken connection edges.
