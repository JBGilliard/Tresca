//! Texture creation. The depth texture (Depth32Float), the swapchain color
//! format handling, and sampler creation. For v0.1, no texture loading from
//! disk — materials are solid colors. v0.3+ will load albedo/normal/roughness
//! textures from PNGs.
