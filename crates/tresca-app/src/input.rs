//! InputState struct tracking which keys are held, mouse position, mouse delta
//! this frame, mouse buttons. Reset per-frame deltas at the end of each frame.
//! Public methods for querying state and consuming events from winit.
